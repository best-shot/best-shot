{
  "name": "best-shot-dev",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=8.10.0"
  },
  "workspaces": [
    "packages/*",
    "website"
  ],
  "devDependencies": {
    "jest": "^24.5.0",
    "nodemon": "^1.18.10",
    "source-map": "^0.7.3"
  },
  "optionalDependencies": {
    "@nice-move/eslint-config-base": "^0.0.2",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.15.3",
    "eslint-plugin-jest": "^22.4.1",
    "eslint-plugin-node": "^8.0.1",
    "lint-staged": "^8.1.5",
    "prettier": "^1.16.4",
    "prettier-plugin-toml": "^0.3.1"
  },
  "scripts": {
    "inspect": "best-shot inspect --stamp test",
    "docs": "cd website && nodemon --config ../nodemon.json",
    "format": "lint-staged",
    "test": "jest"
  }
}
