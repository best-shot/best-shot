const cherryMap = require('./cherry-map');
const imageAvatar = require('./image-avatar');
const injectEruda = require('./inject-eruda');
const removeMomentLocale = require('./remove-moment-locale');
const replaceLodash = require('./replace-lodash');
const srcAlias = require('./src-alias');

module.exports = {
  cherryMap,
  imageAvatar,
  injectEruda,
  removeMomentLocale,
  replaceLodash,
  srcAlias
};
