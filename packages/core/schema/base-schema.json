{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Configuration of best-shot",
  "type": "object",
  "properties": {
    "outputPath": {
      "default": "build",
      "description": "Same as output.path, support [mode]/[mode:shorthand]/[platform]",
      "minLength": 1,
      "type": "string"
    },
    "publicPath": {
      "default": "/",
      "description": "Same as output.publicPath，But also used in other places",
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "outputPath",
    "publicPath"
  ]
}
